
let initialState = {
    friends: [
        {id: 1, name: 'Nikita' },
        {id: 2, name: 'Andrey' },
        {id: 3, name: 'Mihalych' },
    ]
}  ;

const sidebarReducer = (state = initialState) => {

    return state;
}

export default sidebarReducer;